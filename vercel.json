{
  "version": 2,
  "builds": [
    {
      "src": "api/index.ts",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/api/index.ts"
    }
  ],
  "installCommand": "npm install",
  "buildCommand": "cd packages/shared && npm run build && cd ../server && npx prisma generate && npm run build"
}
